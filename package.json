{
  "name": "hydb",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/hydb",
    "packages/hyapp"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces",
    "sandbox": "npm run sandbox --workspace @hyos/hydb"
  }
}
